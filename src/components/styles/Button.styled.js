import styled from "styled-components";

const Button = styled.button`
  background: none;
  font-size: 1.4rem;
  margin: 10px;
  width: 150px;
  height: 36px;
  border-radius: 25px;
`;

export default Button;
